name: Freelankarx SEO Automation

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install jinja2

      - name: List repo files
        run: |
          echo "Root:"
          ls -la
          echo "Templates:"
          ls -la templates || echo "Missing templates"
          echo "Output:"
          ls -la output || echo "Missing output"

      - name: Run generator script
        run: |
          python generator.py || (echo "Script failed" && exit 1)

      - name: Git config
        run: |
          git config --global user.name "Freelankarx SEO Bot"
          git config --global user.email "seo-bot@freelankarx.io"

      - name: Commit and push changes
        run: |
          git add output/
          git diff --cached --quiet && echo "No changes to commit" || (git commit -m "SEO update $(date)" && git push)
